// This file contains any global test setup for the stylelint-oldfashioned-order package

// Import required test utilities from Vitest
import { vi, beforeEach } from 'vitest';

// Setup any global mocks or test environment configurations here

// Reset mocks between tests
beforeEach(() => {
    vi.resetAllMocks();
});
